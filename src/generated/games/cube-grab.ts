/**
 * 🎮 Куб с физикой и управлением лучом
 */

console.log("📦 Куб с физикой!");

// Создаём куб
const cube = createBox([0, 1.2, -1.5], 0xff4444, 0.3);
addPhysics(cube, {
  grabbable: true,  // можно хватать лучом
  dynamic: true     // падает с гравитацией
});

// Подпись
createLabel([0, 2, -1.5], "📦 Хватай куб лучом!");

// Game loop
const updateGame = (dt: number) => {
  // Куб автоматически управляется системой grab
  // Наведи луч + нажми триггер чтобы схватить
};
