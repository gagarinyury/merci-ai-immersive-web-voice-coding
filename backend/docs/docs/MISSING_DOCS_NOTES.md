# Заметки: Чего не хватает в документации IWSDK

## 🔴 ГЛАВНАЯ ПРОБЛЕМА (Beat Saber не работал)

### Ошибка которую я сделал:
```typescript
// ❌ НЕПРАВИЛЬНО - объект НЕ будет grabbable!
const mesh = new THREE.Mesh(geometry, material);
world.scene.add(mesh);  // Добавил напрямую в сцену - БЕЗ ECS!
```

### Правильный способ:
```typescript
// ✅ ПРАВИЛЬНО - объект можно хватать
import { Interactable, DistanceGrabbable } from '@iwsdk/core';

const mesh = new THREE.Mesh(geometry, material);
mesh.position.set(0, 1.5, -2);  // Позиция ДО createTransformEntity!

const entity = world.createTransformEntity(mesh);  // Создать ECS entity
entity.addComponent(Interactable);                 // ОБЯЗАТЕЛЬНО!
entity.addComponent(DistanceGrabbable, {           // Или OneHandGrabbable/TwoHandsGrabbable
  maxDistance: 10,
  translate: true,
  rotate: true
});
```

### Ключевые ошибки:
1. **world.scene.add()** - добавляет в THREE.js сцену, но НЕ в ECS систему
2. **Нет Interactable** - без этого компонента объект не интерактивный
3. **Нет Grabbable компонента** - без этого нельзя хватать

### Что нужно знать:
- `grabbing: true` уже включено в World config (`src/index.ts`)
- Нужно ОБЯЗАТЕЛЬНО использовать `world.createTransformEntity(mesh)`
- Нужно ОБЯЗАТЕЛЬНО добавить компоненты `Interactable` + Grabbable

---

## Проблема
При создании Beat Saber игры пришлось долго искать информацию, которая должна быть легко доступна.

---

## 1. Доступ к XR Input из live-scene.ts

**Что искал:** Как получить доступ к XRInputManager из `live-scene.ts` (без создания ECS системы)?

**Проблема:** В документации описано как использовать `this.input` в системах, но не как получить доступ к input напрямую из world.

**Нужно документировать:**
```typescript
// Как получить XRInputManager?
const world = window.__IWSDK_WORLD__ as World;
// world.input? world.xrInput? world.player.input?
```

**Вопросы:**
- Есть ли `world.input`?
- Или нужно обязательно создавать систему для доступа к XR input?
- Как подписаться на события контроллеров (trigger, grip, thumbstick)?

---

## 2. Прикрепление объектов к контроллерам

**Что нашёл:** В `xr-input.md` есть пример:
```typescript
xrInput.player.gripSpaces.left.add(leftSwordMesh);
xrInput.player.gripSpaces.right.add(rightSwordMesh);
```

**Что НЕ ясно:**
- Как получить `xrInput` из `live-scene.ts`?
- `world.player` это XROrigin, но есть ли у него `gripSpaces`?
- Нужен ли специальный импорт?

**Нужно добавить пример:**
```typescript
// live-scene.ts - как прикрепить меч к контроллеру
const world = window.__IWSDK_WORLD__ as World;
// ??? как дальше
```

---

## 3. Обработка событий контроллеров

**Что искал:** Как реагировать на нажатия trigger/grip для "разрубания" блоков?

**Нужно документировать:**
- События: `squeeze`, `select`, `thumbstick`
- Как подписаться на них из live-scene.ts
- Пример:
```typescript
// Как слушать нажатие trigger?
// controller.addEventListener('selectstart', () => { ... })?
// Или через world.onUpdate проверять состояние?
```

---

## 4. Collision Detection (столкновения)

**Что искал:** Как определить что сабля "разрубила" блок?

**Варианты которые не ясны:**
1. Использовать физику (PhysicsBody) и слушать collision events?
2. Проверять пересечение вручную через raycasting?
3. Использовать THREE.js Box3.intersectsBox()?
4. Есть ли встроенный механизм в IWSDK?

**Нужно документировать:**
- Как детектить столкновения между объектами
- Как получить collision events от PhysicsBody
- Пример простого collision detection без физики

---

## 5. Аудио в IWSDK

**Что искал:** Как проигрывать музыку и звуки для ритм-игры?

**Нужно документировать:**
- Как загрузить и проиграть аудио файл
- Как синхронизировать игровые события с музыкой
- Есть ли AudioComponent или нужно использовать THREE.js Audio напрямую?

---

## 6. Spawn объектов по таймеру

**Что нашёл:** `world.onUpdate((delta) => {...})` - хук для игровой логики

**Что НЕ ясно:**
- Это вызывается каждый кадр?
- `delta` это время в секундах или миллисекундах?
- Можно ли несколько раз вызывать `world.onUpdate`? Они накапливаются?

---

## 7. Структура World API

**Общая проблема:** Нет единой страницы с полным API World:

```typescript
interface World {
  // Что доступно?
  createTransformEntity(object3D): Entity;
  onUpdate(callback: (delta: number) => void): void;
  player: XROrigin; // ???
  input: XRInputManager; // ???
  physics: ???;
  // Что ещё?
}
```

---

## Рекомендации по улучшению документации

### Создать новые страницы:

1. **`guides/xr-controllers-live-scene.md`**
   - Как работать с контроллерами из live-scene.ts (без систем)
   - Прикрепление объектов к рукам
   - Обработка input событий

2. **`guides/collision-detection.md`**
   - Способы детекции столкновений
   - С физикой и без
   - События столкновений

3. **`guides/audio.md`**
   - Загрузка и воспроизведение звуков
   - Пространственный звук в VR
   - Синхронизация с геймплеем

4. **`reference/world-api.md`**
   - Полный список всех методов и свойств World
   - Примеры использования каждого

5. **`examples/beat-saber/`**
   - Полный пример ритм-игры
   - Сабли на контроллерах
   - Spawn блоков
   - Collision detection
   - Scoring

---

## ✅ Найденные ответы

### Grabbable объекты:
```typescript
import { World, Interactable, DistanceGrabbable, OneHandGrabbable, TwoHandsGrabbable } from '@iwsdk/core';
import * as THREE from 'three';

const world = (window as any).__IWSDK_WORLD__ as World;

// 1. Создать mesh
const geometry = new THREE.BoxGeometry(0.3, 0.3, 0.3);
const material = new THREE.MeshStandardMaterial({ color: 0xff0000 });
const mesh = new THREE.Mesh(geometry, material);
mesh.position.set(0, 1.5, -2);  // Позиция ДО createTransformEntity!

// 2. Создать entity (ОБЯЗАТЕЛЬНО!)
const entity = world.createTransformEntity(mesh);

// 3. Добавить компоненты (ОБЯЗАТЕЛЬНО!)
entity.addComponent(Interactable);

// Вариант А: Distance Grabbable (можно хватать издалека лучом)
entity.addComponent(DistanceGrabbable, {
  maxDistance: 10,
  translate: true,
  rotate: true,
  scale: false
});

// Вариант Б: One Hand Grabbable (хватать вблизи одной рукой)
entity.addComponent(OneHandGrabbable, {
  translate: true,
  rotate: true
});

// Вариант В: Two Hands Grabbable (хватать двумя руками + масштаб)
entity.addComponent(TwoHandsGrabbable, {
  translate: true,
  rotate: true,
  scale: true
});
```

### World config (уже настроен в src/index.ts):
```typescript
World.create(container, {
  features: {
    grabbing: true,  // ✅ Включено!
    physics: true,   // ✅ Включено!
  }
});
```

---

*Создано: при попытке реализовать Beat Saber*
*Обновлено: найдена главная ошибка - использовал scene.add вместо createTransformEntity*

---

## 🔴 НОВАЯ ПРОБЛЕМА: Объекты вообще не появляются

### Симптомы:
- Написал простой grabbable куб (правильно через createTransformEntity + Interactable + DistanceGrabbable)
- Объект НЕ появился в сцене
- Beat Saber тоже ничего не показывал

### Возможные причины (нужно проверить):

1. **live-scene.ts не импортируется?**
   - Проверить что в `src/index.ts` или где-то есть `import './generated/live-scene'`
   - Или dynamic import

2. **Vite HMR не работает?**
   - Возможно файл не в watch mode
   - Или ошибка при hot reload

3. **Ошибка в консоли браузера?**
   - Нужно проверить DevTools → Console на ошибки
   - Может быть ошибка импорта компонентов

4. **WebXR сессия не активна?**
   - Может объекты создаются, но камера не там где нужно
   - Или XR session не стартовала

5. **`window.__IWSDK_WORLD__` не определён?**
   - Код выполняется ДО инициализации World
   - Нужен правильный timing

### Что проверить на фронтенде:

```typescript
// 1. Проверить что world существует
console.log('World:', window.__IWSDK_WORLD__);

// 2. Проверить что createTransformEntity работает
const entity = world.createTransformEntity(mesh);
console.log('Entity created:', entity);

// 3. Проверить что mesh добавлен в сцену
console.log('Scene children:', world.scene.children);

// 4. Проверить позицию камеры/игрока
console.log('Player position:', world.player?.position);
```

### Нужно добавить в документацию:
- **Troubleshooting guide** - что проверять когда объекты не появляются
- **Как дебажить live-scene.ts** - логи, DevTools, etc.
- **Порядок инициализации** - когда можно безопасно использовать world

---

*Обновлено: куб тоже не появился - проблема на стороне фронтенда, не в коде IWSDK*
