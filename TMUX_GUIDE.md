# tmux Quick Guide for VRCreator2

## 🚀 Запуск

```bash
npm run start:tmux
```

Откроется tmux с двумя панелями:
- **Верхняя панель** - Backend (port 3001, 3002)
- **Нижняя панель** - Frontend (port 8081)

---

## ⌨️ Основные горячие клавиши

**Все команды начинаются с `Ctrl+B`, затем отпускаешь и нажимаешь следующую клавишу.**

### Навигация между панелями

| Комбинация | Действие |
|------------|----------|
| `Ctrl+B`, `↑` | Переключиться на верхнюю панель (Backend) |
| `Ctrl+B`, `↓` | Переключиться на нижнюю панель (Frontend) |
| `Ctrl+B`, `o` | Переключиться на следующую панель |

### Управление сессией

| Комбинация | Действие |
|------------|----------|
| `Ctrl+B`, `d` | **Detach** - отключиться от сессии (серверы продолжат работать в фоне) |
| `Ctrl+B`, `x` | Убить текущую панель (спросит подтверждение) |
| `Ctrl+B`, `&` | Убить все окно (спросит подтверждение) |

### Прокрутка логов (scroll mode)

| Комбинация | Действие |
|------------|----------|
| `Ctrl+B`, `[` | Войти в режим прокрутки (можно скроллить стрелками, PgUp/PgDown) |
| `q` | Выйти из режима прокрутки |

### Изменение размера панелей

| Комбинация | Действие |
|------------|----------|
| `Ctrl+B`, `Ctrl+↑` | Увеличить текущую панель вверх |
| `Ctrl+B`, `Ctrl+↓` | Увеличить текущую панель вниз |
| `Ctrl+B`, `Alt+1` | Вертикальный layout (одна над другой) |
| `Ctrl+B`, `Alt+2` | Горизонтальный layout (одна рядом с другой) |

---

## 🔄 Работа с detached сессией

**После `Ctrl+B, d` (detach):**

```bash
# Посмотреть список сессий
tmux ls

# Вернуться к сессии
tmux attach -t vrcreator2

# Убить сессию удалённо
tmux kill-session -t vrcreator2
```

---

## 🛑 Как остановить серверы

**Способ 1: Graceful shutdown**
1. `Ctrl+C` в Backend панели → остановит backend
2. `Ctrl+C` в Frontend панели → остановит frontend
3. `Ctrl+B`, `&` → убить окно

**Способ 2: Быстрое завершение**
- `Ctrl+B`, `x` → убить панель (повторить 2 раза)
- Или `Ctrl+B`, `&` → убить всё окно

**Способ 3: Из другого терминала**
```bash
tmux kill-session -t vrcreator2
```

---

## 📖 Примеры использования

**Утром начинаешь работу:**
```bash
npm run start:tmux
# → Backend и Frontend запускаются
# → Работаешь в VR
```

**Хочешь переключиться на другую задачу:**
```
Ctrl+B, d  # Detach - серверы продолжат работать
# → Делаешь другие дела в терминале
```

**Вернуться к разработке:**
```bash
tmux attach -t vrcreator2
# → Видишь все логи как оставил
```

**Закончил работу:**
```
Ctrl+C в обеих панелях
Ctrl+B, &  # Убить окно
```

---

## 🎨 Копирование текста из tmux

**macOS (iTerm2/Terminal.app):**
1. Зажми `Option` (Alt)
2. Выдели мышкой текст
3. `Cmd+C` для копирования

**Альтернатива (tmux copy mode):**
1. `Ctrl+B`, `[` - войти в режим прокрутки
2. Нажми `Space` - начать выделение
3. Стрелками выдели текст
4. `Enter` - скопировать
5. `Ctrl+B`, `]` - вставить

---

## 🔍 Поиск в логах

1. `Ctrl+B`, `[` - войти в режим прокрутки
2. `Ctrl+S` - поиск вперёд (введи текст, Enter)
3. `n` - следующее совпадение
4. `N` - предыдущее совпадение
5. `q` - выйти

---

## 🆘 Полезные команды

```bash
# Посмотреть все горячие клавиши
Ctrl+B, ?

# Перезагрузить tmux конфигурацию (если настроил ~/.tmux.conf)
Ctrl+B, :source-file ~/.tmux.conf

# Показать время в панели
Ctrl+B, t
```

---

## 🎯 Tips & Tricks

**Совет 1:** Если backend упал, перезапусти его прямо в панели:
```
↑ (стрелка вверх) → Enter
# Выполнит последнюю команду (npm run backend)
```

**Совет 2:** Хочешь 3 панели (backend, frontend, logs)?
```
Ctrl+B, %  # Разделить текущую панель вертикально
```

**Совет 3:** Создал случайно лишнюю панель?
```
Ctrl+D  # Закрыть текущую панель
```

---

## 📚 Дополнительно

**Официальная документация:**
- `man tmux` - встроенная документация
- https://github.com/tmux/tmux/wiki

**Шпаргалки:**
- https://tmuxcheatsheet.com/
